const { getSolution } = require('../../src/puzzle-solvers/day7');
const { expect } = require("chai");

describe ('day 7 getSolution', () => {
    let input;

/*     it ('test case', () => {
        input = "?";
        expect(getSolution(input)).to.equal(11);
    }) */

})